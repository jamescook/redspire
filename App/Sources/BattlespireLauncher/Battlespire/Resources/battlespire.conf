# Bundled with the app and passed via -conf on every launch, so Battlespire
# never depends on whatever's in the user's own ambient dosbox-staging.conf
# (that ambient dependency is exactly what caused a real regression: cycles
# left over from a completely different game's tuning silently applied here
# too).
[cpu]
cycles=auto
